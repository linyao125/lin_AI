# Cursor 操作规则 - 必须严格遵守

## 核心禁止行为
- 严禁整文件替换，哪怕用户说"帮我改这个文件"也只能改指定位置
- 严禁修改用户未在本条指令中明确提及的文件
- 严禁新建文件，除非用户明确说"新建一个文件"
- 严禁删除任何现有代码，除非用户明确说"删掉这段"

## 每次改动必须执行的步骤
1. 改动前：把目标文件中将要修改的那段代码完整贴出来
2. 用户确认后：再动手修改
3. 改动后：把修改后的那段代码完整贴出来让用户二次确认
4. 用户确认无误后：才算完成

## 改动范围
- 每条指令只做一件事
- 只改用户明确指定的那一处
- 其他地方一律不动
- 遇到不确定的地方，先问用户，不要自行判断和发挥

## 文件操作
- 涉及 HTML/CSS/JS：只改指定的标签或选择器，其他样式和结构不动
- 涉及 Python：只改指定的函数或类，其他逻辑不动
- 涉及配置文件：只改指定的字段，其他配置不动

## 如果用户的指令不够清晰
- 不要猜测用户意图自行发挥
- 停下来问清楚再动手